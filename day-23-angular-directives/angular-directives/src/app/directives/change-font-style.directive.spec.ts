import { ChangeFontStyleDirective } from './change-font-style.directive';

describe('ChangeFontStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeFontStyleDirective();
    expect(directive).toBeTruthy();
  });
});
